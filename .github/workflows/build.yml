name: Build iOS dylib

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode/SDK
        run: |
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-path

      - name: Build normal dylib + static archive
        run: |
          make clean
          make ARCH=arm64 IOS_DEPLOYMENT_TARGET=15.0
          make ARCH=arm64 IOS_DEPLOYMENT_TARGET=15.0 static
          mkdir -p artifacts
          cp libproxychains_livecontainer.dylib artifacts/
          cp libproxychains_livecontainer.a artifacts/

      - name: Build debug dylib
        run: |
          make clean
          make ARCH=arm64 IOS_DEPLOYMENT_TARGET=15.0 DEBUG=1
          cp libproxychains_livecontainer_debug.dylib artifacts/

      - name: Inspect artifacts
        run: |
          ls -lh artifacts/
          file artifacts/libproxychains_livecontainer*.dylib artifacts/libproxychains_livecontainer.a
          lipo -info artifacts/libproxychains_livecontainer*.dylib || true

      - name: Create release and upload assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/libproxychains_livecontainer.dylib
            artifacts/libproxychains_livecontainer_debug.dylib
            artifacts/libproxychains_livecontainer.a
            proxychains.conf
          generate_release_notes: true
          fail_on_unmatched_files: true

      - name: Upload artifact (workflow_dispatch / non-tag)
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v4
        with:
          name: libproxychains_livecontainer-dylibs
          path: |
            artifacts/libproxychains_livecontainer.dylib
            artifacts/libproxychains_livecontainer_debug.dylib
            artifacts/libproxychains_livecontainer.a
            proxychains.conf
